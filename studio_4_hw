function pascal (row, position){
    
    return position > row
        ? 0
        :position === 0 || position === row
        ? 1
        : pascal(row-1,position) + pascal(row-1,position-1);
}

pascal(4,4);

// recursive process