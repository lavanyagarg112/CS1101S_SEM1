// f(x+y) = x(1+xy)^2 + y(1-y) + (1+xy)(1-y)

function square(n){
    return n * n;
}

function f(x, y){
    const a = 1 + x * y;
    const b = 1 - y;
    
    return (x * square(a)) + (y * b) + (1 + x * y)(b);
}

