import {blank, circle, corner, heart, nova, pentagram, rcross, ribbon,
        sail, square, triangle, black, blue, brown, color, green, indigo, 
        orange, pink, purple, random_color, red, white, yellow, 
        beside, beside_frac, flip_horiz, flip_vert, make_cross, overlay,
        overlay_frac, quarter_turn_left, quarter_turn_right, repeat_pattern,
        rotate, scale, scale_independent, show, stack, stack_frac, stackn,
        translate, turn_upside_down} from 'rune';


// My contest entry
function runes_contest(transform, rune) {
    return color(transform(rune),0.55,0.78,0.8);
}

// Keep this show function call
show(runes_contest(make_cross,rcross));