// fibonnaci

//recursive

function fib(n){
    return n<=1
    ? n
    : fib(n-1) + fib(n-2);
} 


// iterative

function fib2(n){
    return iter(1,0,n);
    
}

function iter(a,b,count){
    return count===0
    ? b
    : iter(a+b,a,count-1);
}

fib2(5);