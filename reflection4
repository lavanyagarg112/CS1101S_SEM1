display(5, 'the explanation:');

// returns 5
// display second part ?

const p = draw_data(pair(pair(x => x+2, false), pair('string', pair(5,null))));
head(p);

// list : a list is null or a pair whose tail is a null -> recursive definition
// base case: null 

// list notation: display_list

const lst = pair(pair(1, pair(2, null)), 7); // not a list as the tail is not a list

display_list(lst); // only the head is the list